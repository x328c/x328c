import {Text,View} from '@tarojs/components';import Taro,{useDidShow} from '@tarojs/taro';import{useState}from'react';import{Empty,RideCard,Skeleton}from'@/components';import{rideService}from'@/services/rides';import type{RideSummary}from'@/types/api';import'./index.scss';
export default function MyRides(){const [type,setType]=useState<'created'|'joined'>('created');const[items,setItems]=useState<RideSummary[]>([]);const[loading,setLoading]=useState(true);const load=async(t=type)=>{setLoading(true);try{setItems((await rideService.mine(t)).list)}catch{}finally{setLoading(false)}};useDidShow(()=>void load());return <View className="my-list"><View className="my-list__tabs"><Text className={type==='created'?'my-list__on':''} onClick={()=>{setType('created');void load('created')}}>我发起的</Text><Text className={type==='joined'?'my-list__on':''} onClick={()=>{setType('joined');void load('joined')}}>我报名的</Text></View>{loading?<Skeleton/>:items.length?items.map(x=><RideCard key={x.id} ride={x} onClick={()=>Taro.navigateTo({url:`/pages/rides/detail/index?id=${x.id}`})}/>):<Empty text="暂无同行"/>}</View>}
