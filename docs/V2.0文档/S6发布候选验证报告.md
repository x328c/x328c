# S6 发布候选验证报告

日期：2026-08-02  
范围：本地/测试环境；未部署生产、未执行生产迁移、未提交小程序审核。

本轮数据库证据来自现有本地 MySQL 8 内的临时数据库 `jiangxing_local_test`，迁移与 E2E 完成后已删除；未创建新容器，未读取或复制生产数据。

## 结论

**No-Go（可保留内部测试/缩范围候选）**。代码检查没有发现 P0，但 Go 条件仍不满足：

1. 生产数据副本、真实监控告警触达、运营/内容负责人和论坛类目/资质均未在本地验证。

建议仅发布后端/管理后台测试环境候选；论坛默认 `forum.enabled=false`、`forum.write_enabled=false`，小程序使用 4 Tab。

## P0 回归矩阵

| 链路 | 结果 | 证据/原因 |
|---|---|---|
| V1 健康检查、请求 ID、兼容错误 envelope | 通过 | `app.e2e-spec.ts` 1/1；foundation 集成 3/3 |
| V1 登录→约骑→报名→通知 | 通过 | 新增 `v1-core-flow.e2e-spec.ts`，隔离 MySQL 真实写入/通知查询 |
| V2 路线发布→浏览→收藏→相关约骑→下架 | 通过 | `route-flow.e2e-spec.ts`，隔离 MySQL |
| V2 法规导入→复核→发布→搜索→反馈→修订 | 通过 | `regulation-flow.e2e-spec.ts`，隔离 MySQL |
| V2 论坛审核→回复/点赞→举报→下架 | 通过 | `forum-flow.e2e-spec.ts`，隔离 MySQL |
| 静态路由与 `:id` 顺序 | 通过 | `common/route-order.spec.ts` |
| 未审核内容不可公开、XSS/恶意图片 | 通过（单元/既有集成） | sanitizer、moderation、database-safety 测试 |

## 自动化结果

- 后端 build/lint：通过。
- 后端单元：29 suites / 84 tests，通过。
- 后端集成：3 suites / 11 tests，通过（request foundation、resilience、database safety）。
- 后端 E2E：5 suites / 8 tests 通过（V1 冒烟、路线、法规、论坛和健康检查），使用隔离 MySQL。
- 小程序 TypeScript typecheck：通过。
- 管理后台 build/lint：通过；既有 hooks 与大 chunk 警告已消除，最大 chunk 400.28 kB。
- 小程序 4 Tab：通过，Tab/路由/图标校验通过。
- 小程序 5 Tab：通过，Tab/路由/图标校验通过。
- 构建脚本使用 Taro `--no-check` 绕过已知 macOS 原生 doctor panic，并执行独立 app.config、Tab、路由和图标校验；CI 仍应单独运行 doctor。
- 与 CI 一致的 test 模式 4/5 Tab 构建也已串行通过。
- `.github/workflows/ci.yml` 已加入独立 `npx taro doctor` 步骤，用于在 CI runner 检查完整 Taro 配置；本机仅使用 `--no-check` 避免已知 macOS 原生服务异常。
- 包体预算检查：5 Tab 产物主包 940,215 bytes，`packageForum` 27,774、`packageRegulations` 23,258、`packageRoutes` 11,758，均低于预算。

## 迁移、校验与恢复

生产数据副本不可用且未请求授权，因此使用隔离 MySQL 8 测试替身：

| 场景 | 结果 |
|---|---|
| 空库全量 8 迁移 | 通过，`real 1.57s` |
| V1+S1-S4 快照→S5 增量 | 通过，`real 0.45s` |
| 二次 migrate deploy | `Database schema is up to date` |
| 新表校验 | `analytics_events`、`task_failures` 存在，初始行数为 0 |

2026-08-02 另完成本地开发库收敛：旧 `jiangxing_dev` 先备份，再以幂等 seed 将必要虚构 fixture 合入 `jiangxing_db`；连续两次执行计数稳定，原有 V1 约骑和报名数据保留。旧 dev Compose、数据卷和停止状态开发容器已精确删除。

失败恢复策略：迁移前备份并记录 migration 状态；失败保持应用旧版本和 V2 开关关闭，修复后重新前滚；不回滚历史迁移、不执行破坏性 down migration。该策略已文档化，但未在生产副本上演练。

## 开关、缓存与回滚演练

- FeatureFlag 默认安全关闭：route/regulation/forum 均 false，论坛 publish mode 为 invite_only；单元测试通过。
- 论坛关闭/只读路径、限流、幂等、审核异常保持 PENDING 已有单元/集成覆盖；真实服务故障积压和人工补偿未连接外部审核服务。
- Redis key 使用 `v2:` 前缀、TTL 抖动、写后失效、分布式锁均有单测；未连接生产 Redis，未做真实缓存泄露演练。
- 应用回滚手册与开关关闭步骤见 [S5回滚手册.md](./S5回滚手册.md)。

## 运营与治理演练状态

- 内容审核异常：代码路径记录失败、重试、PENDING 和人工补偿；真实供应商超时/积压未演练。
- 法规错误更正：修订模型和审计日志已实现；未使用真实法规数据执行运营演练。
- 举报升级：V1 report 扩展和 SLA 指标已实现；真实值班升级链路未触达。
- 管理员误操作：权限/二次确认/审计代码已覆盖；未在真实后台账号体系执行演练。

## 监控和值班

看板指标：API error rate/p95、慢查询、审核超时/积压、任务失败、法规复核逾期、举报 SLA、计数对账修复量。  
建议阈值：API 5xx >2%（5 分钟）、p95 >800ms（10 分钟）、审核 PENDING >15 分钟或积压 >100、任务失败连续 3 次、法规逾期 >0、举报 SLA 逾期 >0 即升级。

本地已验证指标采集和后台维护入口；真实告警触达、值班人、回滚负责人尚未指定，因此 Go 条件不满足。进入 S6 灰度前必须登记：运营/内容负责人、当班工程师、回滚负责人及电话/IM 通道。

## 缺陷清单

| 等级 | 缺陷 | 状态 |
|---|---|---|
| P0 | 未发现代码级 P0 | 0 |
| P1 | 生产监控/资质/负责人未确认 | 阻塞正式发布 |
| P2 | 真实流量 P95/P99 未建立 | 非阻断但需真实环境观测 |
| P3 | 未新增 | 0 |

## 发布建议

当前仅建议“后端 + 管理后台测试环境候选”，V2 受控功能全部关闭，4 Tab 构建。完成 CI/真机 4/5 Tab 构建、测试数据库三条 V2 E2E、备份恢复演练、真实告警触达、运营负责人和论坛资质确认后，重新进行 Go/No-Go 评审。
