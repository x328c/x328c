# 摩搭子助手 V2.0 后续完善执行计划

日期：2026-08-02  
执行环境：现有本地 Docker MySQL/Redis，不再创建新的隔离沙箱  
发布边界：未获明确授权前不提交、推送、部署、执行生产迁移或提交小程序审核

## 1. 当前结论

V2.0 产品功能已经冻结，后续只做基线整理、缺陷修复、真实环境验证和发布准备。代码级 P0 暂未发现，但生产数据副本、真机、真实告警、内容负责人和论坛类目/资质仍未验证，因此正式发布结论保持 **No-Go**；内部本地开发可以使用 5 Tab 和完整 V2 开关。

## 2. 环境收敛原则

- 唯一本地开发依赖使用根目录 `docker-compose.yml`：MySQL `127.0.0.1:3306/jiangxing_db`、Redis `127.0.0.1:6379`。
- 不再创建 `jiangxing_dev`、临时测试库容器或新的 Docker 卷。
- 旧隔离环境中的必要内容只同步虚构、可审查、可重复生成的开发 fixture；不复制 token、手机号、精确位置、真实法规/帖子全文或外部密钥。
- 数据合并前备份，使用 Prisma 增量迁移和幂等 seed，不用裸 SQL 覆盖本地 V1 数据。
- 同步验证完成后删除旧隔离容器、卷和 `docker-compose.dev.yml`。
- 自动化 E2E 后续如需数据隔离，优先在现有 MySQL 实例内创建专用测试数据库；执行完删除测试数据库，不创建新的 Docker 环境。

## 3. 执行阶段

| 阶段 | 工作 | 交付物 | 退出条件 |
| --- | --- | --- | --- |
| P0 环境收敛 | 双库盘点、备份、fixture 同步、删除旧沙箱 | 本文迁移记录、本地库校验、Docker 资源清单 | 本地 V1 数据保留，V2 fixture 可用，旧沙箱资源不存在 |
| P1 基线整理 | 审查未提交改动、敏感信息、迁移链和文档矛盾 | 可提交变更分组、更新后的冻结/缺陷清单 | 无密钥和无关文件，历史迁移未修改 |
| P2 自动化回归 | 后端单元/集成/E2E、三端 lint/build、4/5 Tab、包体预算 | 可复现命令与结果 | 本次引入问题清零，V1/V2 P0 通过 |
| P3 内容与运营数据 | 官方路线、法规来源、30 关键词命中率、双人复核 | 内容验收表和搜索报告 | 来源、版本、复核与版权信息完整 |
| P4 外部联调 | 微信登录/订阅消息、COS、文图审核、告警触达 | 失败演练与触达记录 | 审核异常保持 PENDING，告警到人 |
| P5 真机与迁移 | 4/5 Tab 真机、弱网、生产脱敏副本迁移/恢复 | 真机矩阵、迁移与恢复报告 | P0/P1 清零，恢复和前滚通过 |
| P6 Go/No-Go | 资质、负责人、开关和回滚评审 | 发布决策记录 | 所有 Go 条件满足，否则缩范围或 No-Go |

## 4. 近期优先任务

1. 完成本地环境收敛并删除旧沙箱。
2. 更新 S5/S6 文档中过期结论：Sass 和后台大 chunk 警告已解决；本地开关全开不代表生产授权。
3. 审查当前大规模未提交改动，形成建议提交分组，不自动提交。
4. 在现有本地 MySQL 中执行三条 V2 E2E 和 V1 冒烟，不新建 Docker 环境。
5. 补充真实法规关键词与官方路线内容；缺少内容负责人时不伪造正式数据。

## 5. 外部阻塞项

- 小程序主体备案及论坛服务类目/资质书面结论。
- 微信、COS、文字/图片审核测试密钥和回调配置。
- 运营/内容负责人、值班工程师、回滚负责人及触达渠道。
- 脱敏生产数据副本和备份恢复授权。
- 微信开发者工具及真机验收记录。

上述任一发布 Go 条件未满足时，论坛正式构建使用 4 Tab，`forum.enabled=false`、`forum.write_enabled=false`。

## 6. 环境迁移记录

2026-08-02 已完成 P0 环境收敛：

- 旧隔离库备份：`/private/tmp/jiangxing_dev_pre_merge_20260802.sql`，62,778 bytes，SHA-256 `9a25f7e8008eb84c708200adf06495ac6f01deb84fb2062b380cf16c6ccdfc51`。
- 本地主库合并前备份：`/private/tmp/jiangxing_db_pre_fixture_merge_20260802.sql`，65,993 bytes，SHA-256 `dac4201ecf6e40a925aead5a04e9c212160f9a120a85d66637b0a85ea3807932`。
- 两库均为 8 条迁移；本地主库无待应用迁移。未导入整库 SQL，只用 `backend/scripts/seed-dev.ts` 幂等写入虚构 V2 fixture。
- seed 连续执行两次后计数稳定：路线 1、点位 3、法规 1、修订 1、板块 4、帖子 2；本地原有约骑 1、报名 1 保留。
- 通过管理 API 恢复路线、法规、论坛浏览/写入开关和 `forum.publish_mode=all`，审计 request_id 为 `local-v2-sandbox-merge-20260802`；三个公开入口均返回 HTTP 200。
- 已删除旧 `docker-compose.dev.yml`、旧 dev MySQL/Redis 数据卷，以及此前开发尝试留下的 10 个停止状态 MySQL/Redis 容器及其匿名卷；未执行全局 prune。
- 当前只保留 `jiangxing-mysql`、`jiangxing-redis` 和主卷 `jiangxing_mysql_data`、`jiangxing_redis_data`。

## 7. 本轮自动化证据

- 后端：29 suites / 84 tests；build、lint 通过。
- 后端 E2E：同一 MySQL 实例的临时 `jiangxing_local_test` 空库应用 8 条迁移，5 suites / 8 tests 通过，测试后数据库已删除。
- 管理后台：build、lint 通过；最大产物 chunk 400.28 kB，无大 chunk 警告。
- 小程序：typecheck、4 Tab/5 Tab production build、两次包体预算检查通过；5 Tab 主包 940,215 bytes。

P0、P1、P2 已在本地可执行范围完成。P3-P6 依赖真实内容、外部服务、真机、脱敏生产副本和负责人决策，未经授权不执行。
