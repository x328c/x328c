# 自治区道路交通违法行为行政处罚裁量权基准数据库导入记录

执行日期：2026-08-08  
目标环境：本地 `jiangxing_db`  
数据边界：每一行创建独立法规草稿及版本 1 草稿修订，不提交复核、不发布

## 源文件与转换结果

- 源工作簿：`自治区道路交通违法行为行政处罚裁量权基准.xlsx`
- 源区域：`Sheet1!A1:P350`
- 表头：第 3 行
- 有效数据：第 4-349 行，序号 1-346
- 第 350 行为统一说明，不作为数据行
- 输出 CSV：346 行、20 列、665,442 bytes
- 唯一 title：346
- 文号：全部为 `新公规〔2025〕1号`
- 适用地区：`650000:新疆维吾尔自治区`

## 后台预览与确认

- 导入任务 ID：`2`
- 幂等键：`discretion-346-20260808-v1`
- 预览 request_id：`discretion-preview-20260808`
- 预览结果：346 行有效、0 错误
- 确认 request_id：`discretion-import-20260808`
- 导入结果：346 条法规草稿、346 个版本 1 草稿修订
- 法规 ID 范围：`4-349`
- 审计日志 ID：`8`

## 数据库核验

- 346 条法规状态均为 0（草稿）。
- 346 个修订状态均为 0（草稿）。
- 346 个 title 全部唯一。
- 正文长度范围为 183-851 字符。
- 每条修订快照包含 2-3 个去重后的标签，以及 1 个新疆地区；CSV 本身仍严格保留用户指定的三个标签片段。
- 导入行错误 JSON 均为 null。
- 数据库中同文号共 347 条：此前导入的完整通知草稿 1 条，加本次裁量基准明细 346 条。
- 后端完整单元测试：30 suites / 88 tests，通过；后端 build、lint 通过。

导入器重复判断已调整为“同发布机构 + 同规范化标题”，以支持同一规范性文件文号下的多个附件明细；Prisma 文号字段本身不是唯一约束。发布仍需另一管理员逐条复核，且不得把该信息索引作为个案处罚结论。

## 2026-08-08 精简标签后替换导入

- 删除前审计：旧任务 ID `2` 精确关联法规 ID `4-349`，346 条法规和修订均为草稿；无反馈、无批次外替代引用，文号和发布机构全部匹配。
- 删除结果：事务删除旧任务及 346 条旧法规，未删除历史审计；新增回撤审计日志 ID `9`。
- CSV 更新：`tags` 改为违法类型、车辆/道路场景和处罚形式的精简检索词；共 136 个标签值，每条 2-7 个，最长 24 字。
- `change_note` 更新：逐行使用完整违法代码生成 `由《自治区道路交通违法行为行政处罚裁量权基准》{违法行为}号违法行为行结构化导入，待另一管理员复核后发布。`。
- 新文件 SHA-256：`768dc34687bbb489482ffc4dffd22a38eaa595ad919b71de97e4c3f7cbc5feb8`，大小 630,999 bytes，346 行、20 列。
- 重新导入任务 ID：`3`；导入审计日志 ID：`10`；新法规 ID：`350-695`。
- 重新导入结果：346 行有效、0 错误，346 条法规和修订均为草稿。
- 数据库逐行复核：精简标签差异 0、新 `change_note` 差异 0、旧 Excel 行号说明残留 0、旧长基础标签残留 0。
